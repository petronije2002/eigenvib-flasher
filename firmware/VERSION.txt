EigenVib firmware — PRODUCTION build
version: prod-faza4e (BURST + MQTT batching)
date:    2026-09-09
source:  sensor_module @ feat/hetero (bb90911)

Batch više chunk-ova u JEDNU MQTT poruku — pravo usko grlo je bio root→MQTT
(1 publish/chunk, ~80 ms). ~10× manje poruka. Flešuj SVE nodove. Uz ovo ide
i cloud ingest (već deployovan) koji razume 'chunk_batch'.
