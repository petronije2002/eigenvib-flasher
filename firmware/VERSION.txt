EigenVib firmware — PRODUCTION build
version: prod-faza4f (BURST + MQTT batch + DEFLATE)
date:    2026-09-09
source:  sensor_module @ feat/hetero (4d6c693)

Burst harvest + MQTT batching + DEFLATE kompresija (brzi tdefl, PSRAM). Posle
batching-a smo bandwidth-bound pa kompresija sad pomaže. Flešuj SVE nodove.
Ide uz cloud ingest (već deployovan) koji dekompresuje batch chunk-ove.
