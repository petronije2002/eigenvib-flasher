EigenVib firmware — PRODUCTION build
version: prod-STABLE3 (burst + BINARY batch + TCP window)
date:    2026-09-09
source:  sensor_module @ feat/hetero (4f09297)

Binarni MQTT batch (bez base64/JSON napuhavanja, ~30% manje bajtova) + veći LWIP TCP
prozor (5760→32768) da se podigne TLS plafon. Bez kompresije. Flešuj SVE nodove.
Ide uz cloud ingest (već deployovan) koji čita 'IBAT' binarni batch.
