EigenVib firmware — PRODUCTION build
version: prod-faza4 (BURST harvest)
date:    2026-09-09
source:  sensor_module @ feat/hetero (ebb3015)
sha256:  e5dc87b1e7f1… (sensor_node.bin)

Faza 4 (ADR-0004): ROOT šalje JEDAN harvest_burst po nodu → child izbaci SVE
chunk-ove zaredom (bez zahteva-po-komadu), pa harvest_done; root reparira rupe
ciljano. Uklanja per-chunk request round-trip (glavno usko grlo). Kompresija OFF.

Flešuj SVE nodove (root + decu) ovom verzijom — burst je root-driven, ali svi
treba isto da se ponašaju. Update mode (otključaj "Erase") čuva identitet/PSK.
